import * as React from "react";
import { IAnimationContext } from "@context/AnimationContext/AnimationContext.models.ts";

export const AnimationContext = React.createContext({} as IAnimationContext);